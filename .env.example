# Database Configuration
DB_HOST=localhost
DB_PORT=5432
DB_NAME=member_onboarding
DB_USER=postgres
DB_PASSWORD=password
DB_SSLMODE=disable

# Server Configuration
PORT=8080

# Environment
ENV=development

# CORS — comma-separated list of allowed origins (local dev default)
CORS_ALLOWED_ORIGINS=http://localhost:3000